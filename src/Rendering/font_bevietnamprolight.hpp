#pragma once

// "Be Vietnam Pro" (Light weight), by VietType / Do Viet Trung, distributed
// via Google Fonts under the SIL Open Font License 1.1
// (https://openfontlicense.org). Compiled to DirectXTK's binary .spritefont
// format (Microsoft's MakeSpriteFont tool - the data below starts with that
// format's "DXTKfont" magic header) and embedded as a byte array so it can
// be loaded from memory without shipping a loose asset file. Sourced via
// this project's stand-reference branch, where Stand embeds the same font
// the same way for its own renderer.
namespace YimMenu::Rendering
{
	struct font_bevietnamprolight
	{
		static const char chunk_1[116520];
	};
}
